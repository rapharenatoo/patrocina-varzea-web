import { createTheme } from "@material-ui/core";

export const style = createTheme({
    palette: {
        primary: {
            main: "#ffe600"
        },
        secondary: {
            main: "#121214"
        }
    },
    typography: {
        allVariants: {
            fontSize: "1rem",
        }
    }
    
})